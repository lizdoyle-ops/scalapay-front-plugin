# Base URL for the Scalapay internal API (no trailing slash).
# Required for live GET /orders/{id}/refund and PATCH /orders/{id}/schedule calls.
# Without this, the plugin falls back to mock data and shows a warning banner.
VITE_API_BASE_URL=https://api.scalapay.com/v1
